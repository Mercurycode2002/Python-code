num = x = y = ID = count = 0
num = int(input("Enter ur number\n"))
ID = int(input("Enter a digit\n"))
while num!=0:
    x = num % 10
    if x == ID:
        count+=1
    num = int(num / 10)
print(count)
